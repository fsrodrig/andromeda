export class Post {
    constructor(
        uid?: string,
        titulo?: string,
        autor?: string,
        fecha?: string,
        foto?: string,
        contenido?: string
    ) {}
}